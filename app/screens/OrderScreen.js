import React from 'react';
import { Button, StyleSheet, Text, View } from 'react-native';

const OrderScreen = () => {
    return (
        <Text style={{fontSize: 48 }}>OrderScreen</Text>
      );
}
 
const styles = StyleSheet.create({
    
});
export default OrderScreen;