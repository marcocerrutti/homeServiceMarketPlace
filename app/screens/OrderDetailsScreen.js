import React from 'react';
import { Button, StyleSheet, Text, View } from 'react-native';

const OrderDetails = () => {
    return (
        <Text style={{fontSize: 48 }}>OrderDetails</Text>
      );
}
 
const styles = StyleSheet.create({
    
});
export default OrderDetails;