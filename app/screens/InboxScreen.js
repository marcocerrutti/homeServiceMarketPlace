import React from 'react';
import { Button, StyleSheet, Text, View } from 'react-native';

const InboxScreen = () => {
    return (
        <Text style={{fontSize: 48 }}>InboxScreen</Text>
      );
}
 
const styles = StyleSheet.create({
    
});
export default InboxScreen;